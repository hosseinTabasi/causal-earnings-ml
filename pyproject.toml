[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "causal-earnings-ml"
version = "0.1.0"
description = "Double ML for earnings-announcement effects (scaffold)"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Hossein Tabasi"}]
requires-python = ">=3.11"
dependencies = ["numpy", "pandas", "scikit-learn", "pyyaml", "scipy"]
[project.optional-dependencies]
dev = ["pytest", "ruff"]
dml = ["doubleml"]
[tool.setuptools.packages.find]
where = ["src"]
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
[tool.ruff]
line-length = 100
target-version = "py311"
